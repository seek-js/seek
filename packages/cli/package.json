{
  "name": "@seekjs/cli",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "seek": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown -c tsdown.config.mts src/cli.ts --format esm --dts --out-dir dist",
    "typecheck": "tsc -b --pretty false --noEmit",
    "lint": "biome lint .",
    "format:check": "biome format .",
    "format:fix": "biome format . --write",
    "test": "echo \"skip: package test not implemented\""
  }
}
