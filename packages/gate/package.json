{
  "name": "@seek/gate",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"skip: @seek/gate\"",
    "typecheck": "echo \"skip: @seek/gate\"",
    "lint": "biome lint ../..",
    "format:check": "biome format ../..",
    "format:fix": "biome format ../.. --write",
    "test": "bun test --cwd ../.."
  }
}
