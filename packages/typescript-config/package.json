{
  "name": "@seek/typescript-config",
  "version": "0.0.0",
  "private": "true",
  "scripts": {
    "build": "echo \"skip: no build for config package\"",
    "typecheck": "echo \"skip: no typecheck for config package\"",
    "lint": "echo \"skip: no lint for config package\"",
    "format:check": "echo \"skip: no format check for config package\"",
    "test": "echo \"skip: no test for config package\""
  }
}
