{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "format-and-lint": {},
    "format-and-lint:fix": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}
